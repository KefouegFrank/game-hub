---
deployment:
  tasks:
    # public_html/ is the whole app — nothing lives outside the docroot.
    - export DEPLOYPATH=$HOME/predictloncasino.com
    - /bin/mkdir -p $DEPLOYPATH
    - /bin/cp -R public_html/. $DEPLOYPATH/
    - /bin/chmod 644 $DEPLOYPATH/.htaccess
